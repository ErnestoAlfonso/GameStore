name: CI

on:
  push:
    branches: [ "main" ]

jobs:

  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    - name: Run commit.sh script
      run: |
        chmod +x commit.sh
        ./commit.sh
